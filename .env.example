# Copy this file to .env.local and replace the placeholder value.
# Required for Gemini AI API calls used by the quiz features.
GEMINI_API_KEY=git add .env.example
git commit -m "Remove API key from .env.example"
